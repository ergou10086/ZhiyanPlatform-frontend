# 🏗️ 前端项目结构整理指南

## 📁 当前项目结构分析

### ✅ 良好的结构
- `src/` - 源代码目录
- `src/components/` - 组件目录
- `src/views/` - 页面视图目录
- `src/api/` - API接口目录
- `src/router/` - 路由配置
- `src/store/` - 状态管理
- `src/utils/` - 工具函数
- `src/config/` - 配置文件

### ⚠️ 需要整理的问题
1. 根目录有太多测试文件
2. 缺少统一的样式管理
3. 组件分类不够清晰
4. 缺少文档和规范

## 🎯 建议的整理方案

### 1. 清理根目录测试文件
```
front/
├── src/                    # 源代码
├── public/                 # 静态资源
├── dist/                   # 构建输出
├── node_modules/          # 依赖包
├── package.json           # 项目配置
├── vue.config.js          # Vue配置
├── README.md              # 项目说明
└── docs/                  # 文档目录（新建）
    ├── api/               # API文档
    ├── components/        # 组件文档
    └── guides/            # 开发指南
```

### 2. 优化 src 目录结构
```
src/
├── main.js                # 入口文件
├── App.vue                # 根组件
├── api/                   # API接口
│   ├── auth.js           # 认证相关API
│   ├── project.js        # 项目相关API
│   ├── knowledge.js      # 知识库相关API
│   └── index.js          # API统一导出
├── assets/               # 静态资源
│   ├── images/           # 图片资源
│   ├── styles/           # 样式文件
│   │   ├── variables.css # CSS变量
│   │   ├── global.css    # 全局样式
│   │   └── components/   # 组件样式
│   └── icons/            # 图标资源
├── components/           # 通用组件
│   ├── common/          # 通用组件
│   │   ├── Button.vue
│   │   ├── Input.vue
│   │   └── Card.vue
│   ├── layout/          # 布局组件
│   │   ├── Header.vue
│   │   ├── Footer.vue
│   │   ├── Sidebar.vue
│   │   └── RightSidebar.vue
│   └── business/        # 业务组件
│       └── GlobalUserProfile.vue
├── views/               # 页面视图
│   ├── auth/           # 认证相关页面
│   │   ├── Login.vue
│   │   ├── Register.vue
│   │   └── ForgotPassword.vue
│   ├── project/        # 项目相关页面
│   │   ├── ProjectSquare.vue
│   │   ├── ProjectCreate.vue
│   │   ├── ProjectDetail.vue
│   │   └── ProjectKnowledge.vue
│   ├── knowledge/      # 知识库相关页面
│   │   ├── KnowledgeBase.vue
│   │   ├── KnowledgeBaseAI.vue
│   │   ├── KnowledgeBaseCabinet.vue
│   │   └── KnowledgeBaseCatalog.vue
│   ├── user/           # 用户相关页面
│   │   └── Profile.vue
│   └── common/         # 通用页面
│       ├── Home.vue
│       └── AIAssistant.vue
├── router/             # 路由配置
│   ├── index.js        # 主路由
│   └── modules/        # 路由模块
│       ├── auth.js     # 认证路由
│       ├── project.js  # 项目路由
│       └── knowledge.js # 知识库路由
├── store/              # 状态管理
│   ├── index.js        # Store入口
│   ├── modules/        # Store模块
│   │   ├── auth.js     # 认证状态
│   │   ├── user.js     # 用户状态
│   │   └── project.js  # 项目状态
│   └── types.js        # 状态类型定义
├── utils/              # 工具函数
│   ├── auth.js         # 认证工具
│   ├── request.js      # 请求工具
│   ├── validation.js   # 验证工具
│   └── common.js       # 通用工具
├── config/             # 配置文件
│   ├── index.js        # 主配置
│   └── env.js          # 环境配置
└── constants/          # 常量定义
    ├── api.js          # API常量
    ├── routes.js       # 路由常量
    └── messages.js     # 消息常量
```

## 🚀 实施步骤

### 第一步：创建新的目录结构
1. 创建 `docs/` 目录
2. 创建 `src/constants/` 目录
3. 在 `src/components/` 下创建子目录
4. 在 `src/views/` 下创建子目录
5. 在 `src/store/` 下创建 `modules/` 目录

### 第二步：移动和重组文件
1. 移动测试文件到 `docs/` 目录
2. 重组组件文件
3. 重组页面文件
4. 创建新的工具文件

### 第三步：更新导入路径
1. 更新所有文件的导入路径
2. 更新路由配置
3. 更新组件引用

### 第四步：创建文档
1. 组件使用文档
2. API接口文档
3. 开发规范文档

## 📋 具体整理任务

### 1. 清理根目录
- [ ] 移动所有测试HTML文件到 `docs/tests/`
- [ ] 移动文档文件到 `docs/`
- [ ] 移动原型图到 `docs/prototypes/`

### 2. 重组组件
- [ ] 移动通用组件到 `components/common/`
- [ ] 移动布局组件到 `components/layout/`
- [ ] 移动业务组件到 `components/business/`

### 3. 重组页面
- [ ] 移动认证页面到 `views/auth/`
- [ ] 移动项目页面到 `views/project/`
- [ ] 移动知识库页面到 `views/knowledge/`
- [ ] 移动用户页面到 `views/user/`

### 4. 优化工具函数
- [ ] 创建 `utils/request.js` 统一请求处理
- [ ] 创建 `utils/validation.js` 统一验证
- [ ] 创建 `utils/common.js` 通用工具

### 5. 创建常量文件
- [ ] 创建 `constants/api.js` API常量
- [ ] 创建 `constants/routes.js` 路由常量
- [ ] 创建 `constants/messages.js` 消息常量

## 🎨 样式管理优化

### 1. 创建样式架构
```
src/assets/styles/
├── variables.css        # CSS变量
├── global.css          # 全局样式
├── components/         # 组件样式
│   ├── button.css
│   ├── input.css
│   └── card.css
├── layouts/           # 布局样式
│   ├── header.css
│   ├── footer.css
│   └── sidebar.css
└── pages/             # 页面样式
    ├── auth.css
    ├── project.css
    └── knowledge.css
```

### 2. 创建主题系统
- 支持明暗主题切换
- 统一的颜色系统
- 响应式设计规范

## 📚 文档系统

### 1. 组件文档
- 组件使用说明
- 属性说明
- 示例代码

### 2. API文档
- 接口说明
- 参数说明
- 响应格式

### 3. 开发规范
- 代码规范
- 命名规范
- 提交规范

## 🔧 工具和配置

### 1. 开发工具
- ESLint配置
- Prettier配置
- Git hooks

### 2. 构建优化
- 代码分割
- 懒加载
- 缓存策略

## 📈 性能优化

### 1. 代码优化
- 组件懒加载
- 路由懒加载
- 图片优化

### 2. 构建优化
- 代码分割
- 压缩优化
- 缓存策略

这个整理方案将让您的前端项目更加规范、可维护和可扩展。您希望我帮您实施哪个部分？
